{% doc %}
  Renders product price with compare-at and sale styling.

  @param {product} product - The product object

  @example
  {% render 'price', product: product %}
{% enddoc %}

{%- liquid
  assign on_sale = false
  if product.compare_at_price > product.price
    assign on_sale = true
  endif

  assign price_varies = false
  if product.price_varies
    assign price_varies = true
  endif
-%}

<div class="price{% if on_sale %} price--on-sale{% endif %}">
  {%- if price_varies -%}
    <span class="price__from">
      {{ 'products.product.from_price' | t: price: product.price_min | money_without_trailing_zeros }}
    </span>
  {%- else -%}
    <span class="price__regular">
      {{ product.price | money_without_trailing_zeros }}
    </span>
  {%- endif -%}

  {%- if on_sale -%}
    <span class="price__compare">
      <s>{{ product.compare_at_price | money_without_trailing_zeros }}</s>
    </span>
  {%- endif -%}
</div>
