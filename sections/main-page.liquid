<div class="page section-padding">
  <div class="container">
    {%- render 'breadcrumb' -%}
    <h1 class="page__title h1 text-center">{{ page.title }}</h1>
    <div class="page__content rte" style="max-width: 800px; margin: 0 auto;">
      {{ page.content }}
    </div>
  </div>
</div>

{% schema %}
{ "name": "Page", "settings": [] }
{% endschema %}
